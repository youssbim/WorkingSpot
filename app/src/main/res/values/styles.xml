<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="UsernameTextAppearance" parent="TextAppearance.MaterialComponents.Subtitle1">
        <!-- Proprietà generali -->
        <item name="android:textSize">@dimen/title_size</item>
        <item name="android:textColor">@color/md_theme_onPrimaryContainer</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">true</item>
    </style>
    <style name="CustomRatingBar" parent="@style/Widget.AppCompat.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_progress</item>
    </style>
    <!-- Stile personalizzato per il EditText interno della SearchView -->
    <style name="CustomSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@null</item>
        <item name="searchIcon">@drawable/baseline_search_24</item>
        <item name="android:background">@drawable/rounded_shape</item>
    </style>
</resources>